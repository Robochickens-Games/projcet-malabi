# public/img/ is generated by scripts/generate-assets.mjs from brain/images
# concepts, but we COMMIT it so Vercel's git-triggered builds are self-contained
# (the build host has no access to the brain source images / generation pipeline).
node_modules/
dist/
.vercel
