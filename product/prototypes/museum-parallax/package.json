{
  "name": "museum-parallax-prototype",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Science Museum Mystery — parallax feel prototype: lobby tooth → dinosaur hall → match the diorama via the catalog",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "verify": "node scripts/verify-wings.mjs && node scripts/verify-economy.mjs && node scripts/verify-spacewing.mjs && node scripts/verify-solar.mjs"
  },
  "dependencies": {
    "gsap": "^3.12.7",
    "pixi.js": "^8.6.6",
    "three": "^0.160.1"
  },
  "devDependencies": {
    "playwright-core": "^1.60.0",
    "vite": "^6.0.0"
  }
}
