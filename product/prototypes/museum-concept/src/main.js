import './ui-3d.css'      // shared Toon-3D design system (tokens + components)
import './style.css'      // prototype-specific chrome, builds on the DS tokens
import { boot } from './engine.js'

boot()
