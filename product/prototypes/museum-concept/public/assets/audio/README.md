# audio/ — sound (optional, not wired yet)

Drop `.mp3`/`.ogg` here (taps, pickups, ambience). Audio isn't hooked into the
engine yet — add a small play() helper in `engine.js → runAction()` when you
want a hotspot to trigger a sound.
